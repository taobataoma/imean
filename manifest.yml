---
declared-services:
  mean-mysql:
    label: mysqldb
    plan: 100
applications:
- name: mean
  host: mean-${random-word}
  memory: 128M
  services:
  - mean-mysql
  env:
    NODE_ENV: cloud-foundry